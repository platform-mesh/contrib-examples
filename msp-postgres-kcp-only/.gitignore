.worktrees/

# Runtime state — created by `task up` (not source).
# `.kcp/`  : host-binary kcp's data dir (admin kubeconfig, etcd, certs, PID, log).
# `.kube/` : kind cluster's kubeconfig (`kind.kubeconfig`).
# `bin/`   : downloaded kcp / kubectl-kcp / kubectl-ws binaries.
.kcp/
.kube/
bin/
